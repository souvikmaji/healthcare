**Mongo DB**
$ sudo service mongod start

$ mongo
> show dbs
> use healthcare
> db: current database
> db.help(): list of commands
> db.stats()
> show collections
> db.patient_collection.find()

$ sudo service mongod stop

**Docker**
$ sudo systemctl start docker
or
$ sudo service docker start

$ sudo docker-compose build
$ sudo docker-compose up